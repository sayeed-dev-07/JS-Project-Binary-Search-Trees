import Node from "./node.js";

export default class Tree{
    constructor(arr){
        this.arr = arr;
        this.root = null;
    }
    buildTree(arr){
        this.#sortArray(arr)
    }
    #sortArray(arr){
        let newSortedArr = [];

    }
}
